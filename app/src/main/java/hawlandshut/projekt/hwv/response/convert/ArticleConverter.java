package hawlandshut.projekt.hwv.response.convert;

/**
 * Created by Mansi on 08.03.2017.
 */
public class ArticleConverter {


   /* public static ArticleElement convert(Article article) {
        ArticleElement element = new ArticleElement();
        element.setId(article.getId());
        element.setBarcodeId(article.getBarcodeId());
        element.setMinOrderAmount(article.getMinOrderAmount());
        element.setName(article.getName());
        element.setUnit(article.getUnit());
        return element;
    }*/
}
